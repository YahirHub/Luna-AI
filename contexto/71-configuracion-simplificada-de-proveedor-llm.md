# 71 — Configuración simplificada de proveedor LLM

/setup-provider solicita URL base y API key. Luna deriva /models y /chat/completions, tolera URLs pegadas con endpoints completos, consulta el catálogo y obliga a elegir por número el modelo antes de guardar.
